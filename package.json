{
  "name": "my-islami-site",
  "version": "1.0.0",
  "description": "Islamic Platform with Node.js and React",
  "main": "server/server.js",
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
